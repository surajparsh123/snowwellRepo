import { memo } from 'react';
import React from 'react';

import resets from '../_resets.module.css';
import classes from './Tokenomic.module.css';

const Tokenomics31 = memo(function Tokenomics31(props = {}) {
  return <div className={`${resets.clapyResets} ${classes.root2}`}></div>;
});

export default Tokenomics31;
